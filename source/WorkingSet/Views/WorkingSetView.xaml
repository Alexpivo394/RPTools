<ui:FluentWindow
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Class="WorkingSet.Views.WorkingSetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    WindowStartupLocation="CenterScreen"
    Title="WorkingSet"
    Height="200"
    Width="350"
    MinHeight="200"
    MinWidth="350"
    ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Создание Рабочих наборов">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="AddCircle24"/>
            </ui:TitleBar.Icon>            
        </ui:TitleBar>

        <ui:TextBlock Grid.Row="1" Text="Выберите раздел для создания" 
               HorizontalAlignment="Center" FontSize="14"/>
        <ui:TextBlock Grid.Row="2" Text="Рабочих наборов" 
               HorizontalAlignment="Center" FontSize="14" Margin="0,5,0,10"/>

        <ComboBox Grid.Row="3" ItemsSource="{Binding Sections, Mode=TwoWay}" 
             SelectedItem="{Binding SelectedSection, Mode=TwoWay}" Width="250"/>

        <ui:Button Grid.Row="4" Content="Создать" 
            Command="{Binding CreateWorksetsCommand}"
            Icon="{ui:SymbolIcon Symbol=Play24}"
            Appearance="Primary"
            Width="100" Height="30" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal">
            <TextBlock Text="Тема:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ui:ToggleSwitch
                IsChecked="{Binding DataContext.DarkTheme, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}, Mode=TwoWay}"
                OffContent="Светлая"
                OnContent="Темная"/>
        </StackPanel>
        
    </Grid>

</ui:FluentWindow>