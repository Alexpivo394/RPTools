<ui:FluentWindow
    x:Class="CreateCover.Views.CreateCoverView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Title="ModelTransplanter"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    Height="400"
    Width="800"
    MinHeight="350"
    MinWidth="800"
    ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="CreateCover" Icon="AddCircle24" />
        
        <!-- Content -->
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Правила переноса параметров" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center" />
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Параметры лотка" FontWeight="Regular" Margin="0,0,0,0" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Параметры крышки" FontWeight="Regular" Margin="0,0,0,0" HorizontalAlignment="Center"/>
            </Grid>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MinHeight="150">
                <ui:ListView
                    ItemsSource="{Binding Models}"
                    HorizontalContentAlignment="Stretch"
                    Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ComboBox  
                                    ItemsSource="{Binding TrayParams}" 
                                    DisplayMemberPath="Name"
                                    SelectedItem="{Binding SelectedTrayParam, Mode=TwoWay}"
                                    Height="35"
                                    Margin="0,0,0,0"/>
                                    
                                    
                                <ComboBox
                                    Grid.Column="1" ItemsSource="{Binding CoverParams}" 
                                    DisplayMemberPath="Name"
                                    SelectedItem="{Binding SelectedCoverParam, Mode=TwoWay}"
                                    Height="35"
                                    Margin="10,0,0,0"/>
                                    
                                <ui:Button
                                    Grid.Column="2"
                                    Icon="{ui:SymbolIcon Symbol=Delete24}"
                                    Command="{Binding DataContext.RemoveModelCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Appearance="Transparent"
                                    VerticalAlignment="Stretch"
                                    Margin="10,0,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
            </ScrollViewer>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <ui:Button Content="Добавить"
                           Icon="{ui:SymbolIcon AddCircle24}"
                           Command="{Binding AddModelCommand}"
                           Appearance="Secondary"
                           Margin="0,0,0,0" />
                    
                <ui:Button Content="Запуск"
                           Icon="{ui:SymbolIcon Rocket24}"
                           Command="{Binding StartCommand}"
                           Appearance="Primary"
                           Margin="10,0,0,0" />
                    
            </StackPanel>
        </Grid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,10">
            <TextBlock Text="Тема:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ui:ToggleSwitch
                IsChecked="{Binding DataContext.DarkTheme, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}, Mode=TwoWay}"
                OffContent="Светлая"
                OnContent="Темная"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>