<ui:FluentWindow
    x:Class="ParamToFinish.Views.ParamToFinishView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Title="ModelTransplanter"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    Height="500"
    Width="600"
    MinHeight="450"
    MinWidth="600"
    ExtendsContentIntoTitleBar="True">
<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="ParamToFinish" Icon="AddCircle24" />
        
        <!-- Content -->
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock  Text="Параметр основной стены" FontWeight="Bold" Margin="10,0,0,10" />
            
            <ComboBox Grid.Row="1" ItemsSource="{Binding WallParameters, Mode=TwoWay}" 
                      SelectedItem="{Binding SelectedWallParameter, Mode=TwoWay}"/>
            
            <TextBlock Grid.Row="2" Text="Параметр отделки" FontWeight="Bold" Margin="10,0,0,10" />
            
            <ComboBox Grid.Row="3" ItemsSource="{Binding WallParameters, Mode=TwoWay}" 
                      SelectedItem="{Binding SelectedFinishParameter, Mode=TwoWay}"/>
            
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
                <ui:Button Content="Запуск"
                           Icon="{ui:SymbolIcon Rocket24}"
                           Command="{Binding StartCommand}"
                           Appearance="Primary"
                           Margin="10,0,0,0" />
                    
            </StackPanel>
        </Grid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,10">
            <TextBlock Text="Тема:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ui:ToggleSwitch
                IsChecked="{Binding DataContext.DarkTheme, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}, Mode=TwoWay}"
                OffContent="Светлая"
                OnContent="Темная"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>